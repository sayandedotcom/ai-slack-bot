"use client";

/**
 * Stub. Task 15 fills this in with the actual `POST /api/runs` create form —
 * this file exists only so `RunList` compiles before that task lands.
 */
export function NewRunDialog(_props: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}) {
  return null;
}
