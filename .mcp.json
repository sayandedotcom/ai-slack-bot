{
  "//": "Cloudflare MCP servers. Auth is browser OAuth, handled by the servers themselves (run /mcp to authenticate) -- no Authorization header and no CF_API_TOKEN. Same pattern as the bundled plugin:cloudflare:* servers. See the README security section: no secret values in the repo, ever.",
  "mcpServers": {
    "cloudflare-api": {
      "type": "http",
      "url": "https://mcp.cloudflare.com/mcp"
    },
    "cloudflare-graphql": {
      "type": "http",
      "url": "https://graphql.mcp.cloudflare.com/mcp"
    }
  }
}
